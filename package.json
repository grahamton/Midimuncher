{
  "name": "midi-playground",
  "private": true,
  "license": "AGPL-3.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "corepack pnpm -C apps/desktop dev",
    "build": "corepack pnpm -r build",
    "test": "corepack pnpm -r test",
    "lint": "corepack pnpm -r lint",
    "format": "corepack pnpm -r format",
    "ci": "corepack pnpm lint && corepack pnpm test",
    "package-beta": "corepack pnpm -C apps/desktop build && corepack pnpm -C apps/desktop package:beta"
  }
}
